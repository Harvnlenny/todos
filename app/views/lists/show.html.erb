<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %><br>
  <%= link_to 'Add tasks', new_item_path(@list) %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', list_path %>
